Use case:
	Take any model pickle file
	host it through docker
	bring up the model
	Use it for the requirement. (call the model from the your application)

Here i have used a very tiny model from capstoneprojects/DecissionTreemodel/tree_clf.pkl project